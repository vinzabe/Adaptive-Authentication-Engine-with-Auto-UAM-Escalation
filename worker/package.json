{
  "name": "adaptive-auth-worker",
  "version": "1.0.0",
  "description": "Adaptive Authentication Engine with Auto-UAM Escalation",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20231218.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}